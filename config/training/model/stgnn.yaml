# @package _global_

#### Model params #############################################################
model:
  name: stgnn
  hparams:
    hidden_size: 32
    emb_size: 16
    temporal_layers: 1
    gnn_layers: 2
    activation: 'elu'

dataset:
  connectivity:
    layout: edge_index  # Changed from csr to edge_index
    include_self: False